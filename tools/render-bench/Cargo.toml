[package]
name = "render-bench"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
lunar = { path = "../../crates/lunar", package = "lunar-engine", features = ["3d"] }
wgpu = { workspace = true }
pollster = { workspace = true }
# golden-frame io; already in the workspace lockfile via other tools
png = "0.18"
log = { workspace = true }
env_logger = { workspace = true }
