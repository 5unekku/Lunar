[package]
name = "behavior-fixture"
version.workspace = true
edition.workspace = true
license = "MPL-2.0"
description = "test fixture cdylib: a rust behavior crate for the hot-reload loader test"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
lunar-core = { path = "../../lunar-core" }
lunar-macros = { path = "../../lunar-macros" }
